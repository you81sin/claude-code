# =========================================================
# CHRONOS 依存パッケージ
# =========================================================
# 設計方針: 「核」は依存ゼロでも起動する（LLM/音声/3Dが無くても落ちない）。
# 必要な機能のレイヤーだけ入れていけばよい。
#
# まず動かすなら ↓ の [1] LLM だけで pon は喋れる。
# 全部入れる必要はない。使う機能の行だけ有効にする。
# 確認用: python tools/smoke_test.py （依存ゼロで通る）
# =========================================================

# --- [1] LLM（これだけで pon は会話できる）---
google-genai          # Gemini（CHRONOS_LLM_PROVIDER=gemini のとき）
# anthropic           # Claude を使うなら有効化（CHRONOS_LLM_PROVIDER=anthropic）

# --- [2] 記憶の高速化（任意）---
# numpy               # 入れると記憶ベクトル計算が速くなる。無くても純Pythonで動く。

# --- [3] 音声再生（ローカルでスピーカー/仮想デバイスに出す）---
# 音声合成は別途 Style-Bert-VITS2 サーバ(http://127.0.0.1:5000)を立てる必要がある
# sounddevice
# soundfile

# --- [4] YouTube ライブ連携 ---
# pytchat                       # 簡易チャット取得（手軽）
# google-api-python-client      # YouTube Data API（スパチャ等の本格連携）
# google-auth-oauthlib

# --- [5] Web検索（pon が「調べてみる！」をやる）---
# ddgs

# --- [6] 画像生成（見た目イラスト）---
# requests
# Pillow

# --- [7] 起動排他制御（任意・無くても os.kill で代替）---
# psutil

# =========================================================
# [8] 3Dアバター生成（重量級・GPU/特殊環境向け・任意）
#   ※ drivers/avatar/ の実験スクリプト用。常用しないなら不要。
# =========================================================
# torch
# trimesh
# rembg
# websocket-client     # ComfyUI連携
# python-docx          # 仕様書docx化スクリプト用
# tsr                  # TripoSR（pip簡単ではない・公式リポジトリから導入）
# bpy                  # Blender as a module（特殊インストール）
